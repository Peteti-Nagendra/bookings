{{template "base" .}}

{{define "content"}}
    <div class="container">
        <div class="row">
            <div class="col">
                <h1>This is the about page</h1>
                <p>Hi Nagendra, Welcome to go lang learning</p>
                <p>This is paragraph for about1</p>

                <p>This came from the template: {{index .StringMap "test"}}</p>

                <p>
                    {{if ne (index .StringMap "remoteIP") ""}}
                    Your remote ip address is {{index .StringMap "remoteIP"}}
                {{else}}
                    I dont know your ip address. Visit the <a href="/">home page</a>
                {{end}}
                </p>

            </div>
        </div>
    </div>
{{end}}